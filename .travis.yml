language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t daylanberry/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true daylanberry/docker-react npm run test
